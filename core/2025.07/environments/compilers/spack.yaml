# This is a Spack Environment file.
#
# It describes a set of packages to be installed, along with
# configuration settings.
spack:
  # add package specs to the `specs` list
  specs:
    - gcc@14.2.0 +binutils ~bootstrap +strip +graphite languages=c,c++,fortran,jit %c=gcc@11.4.0
  view: false
  concretizer:
    unify: true
#  packages:
#    all:
#      require: '%c=llvm@15.0.7'
